# vitepress-theme-curve 重写建议书

> 基于对项目架构、功能、代码质量的全量分析，提出系统性重写建议。

---

## 一、架构重构

### 1.1 用 TypeScript 重写

**现状**：全部使用 JavaScript（`.mjs`/`.js`/`.vue`），无类型系统。

**建议**：
- 全部 `.mjs`/`.js` 改为 `.ts`
- `.vue` 组件启用 `<script setup lang="ts">`
- 为以下核心模块定义类型：
  - `PostData` — 文章 frontmatter 数据结构
  - `ThemeConfig` — 主题配置完整类型
  - `PlayerData`、`SearchResult`、`CommentConfig` 等
- 收益：减少运行时错误，IDE 补全，重构信心

```ts
// 示例：核心类型定义
interface PostData {
  title: string
  date: string
  tags: string[]
  categories: string[]
  top?: boolean
  cover?: string
  description?: string
  references?: { name: string; url: string }[]
  copyright?: boolean
  [key: string]: any
}
```

### 1.2 Store 拆分

**现状**：单个 Pinia store（`store/index.js`）包含 27 个状态属性，覆盖主题、播放器、搜索、设置、滚动等完全不相关的领域。

**建议**：拆分为多个独立 store：

| Store | 职责 | 状态示例 |
|---|---|---|
| `useThemeStore` | 明暗模式、背景、字体、排版 | `themeType`, `fontFamily`, `backgroundType` |
| `useLayoutStore` | 控制面板、菜单、侧边栏可见性 | `controlShow`, `mobileMenuShow`, `tocShow` |
| `usePlayerStore` | 音频播放器 | `playState`, `playerShow`, `playerVolume`, `playerData` |
| `useSearchStore` | 搜索 UI 状态 | `searchShow`, `query`, `results` |
| `useScrollStore` | 滚动跟踪 | `scrollData`, `lastScrollY`, `footerIsShow` |
| `useSettingsStore` | 持久化偏好 | `mdIndent`, `contentWidth`, `bannerType` |

- 每个 store 单独文件，职责清晰
- `settingsStore` 单独配置 `persist`
- 消除 `beforeRestore` 中的手动数据迁移 hack

### 1.3 Layout 系统重写

**现状**：`App.vue` 手写大段 `v-if/v-else` 判断页面类型，295 行包含布局 + 主题管理 + 滚动监听 + 右键菜单 + 复制事件。

**建议**：
- 利用 Vue Router 的命名视图或 VitePress 的 `layout` frontmatter：
  - `HomeLayout` → 首页
  - `PostLayout` → 文章页
  - `PageLayout` → 普通页
  - `NotFoundLayout` → 404
- 每个 Layout 组件只关心骨架（Nav + Footer + 内容区），不关心业务逻辑
- 主题管理、滚动监听等全局逻辑抽到 composable 中，在 Layout 内 `onMounted` 调用

```
layouts/
├── default.vue       # 通用布局
├── home.vue          # 首页布局（带 Banner/侧边栏）
└── post.vue          # 文章布局（带 TOC/评论）
```

### 1.4 路由系统

**现状**：页面类型检测靠字符串匹配 `route.path.includes('/pages/posts/')`，脆弱且难以维护。

**建议**：
- 在 VitePress config 中用 `rewrites` 映射清晰的路由模式
- 或通过 `frontmatter` 标记页面类型（`layout: post`, `layout: page`）
- App.vue 只做 `frontmatter.layout` 的分发

---

## 二、组件拆分与优化

### 2.1 大文件拆分

| 文件 | 行数 | 建议 |
|---|---|---|
| `ContentPreview.vue` | 657 | 拆为 `PostCard.vue`（卡）、`FilterBar.vue`（过滤栏）、`CoverImage.vue`（封面） |
| `Nav.vue` | 594 | 拆为 `NavMenu.vue`（菜单）、`NavActions.vue`（右侧按钮组）、`MobileMenu.vue`（已有但分离） |
| `SvgTo3d.vue` | 741 | 单独文件保留，但如果简单 Logo 可考虑纯 CSS 替代 |
| `RightMenu.vue` | 577 | 拆为 `MenuItemResolver`（类型检测）+ `MenuActions`（各操作组件） |
| `Post.vue` | 404 | 拆为 `PostMeta.vue`、`PostContent.vue`、`PostFooter.vue` |
| `Settings.vue` | 340 | 按分组拆 Panel：`FontPanel`、`BackgroundPanel`、`TypoPanel` |
| `App.vue` | 295 | 布局逻辑 + 主题管理 + 事件监听分到不同 composable |
| `Pagination.vue` | 349 | 可接受，但页码跳转逻辑可抽 composable |
| `Player.vue` | 330 | 可接受 |
| `Banner.vue` | 263 | 拆 `HomeBanner` / `PageBanner` |

### 2.2 Composables 抽取

**现状**：`App.vue` 的 `onMounted` 中密集注册了主题、滚动、右键、复制等事件监听。

**建议**：

```js
// useThemeManager.js
export function useThemeManager() {
  const store = useThemeStore()
  watchEffect(() => {
    document.documentElement.className = store.themeValue
    // 字体、缩进、对齐、宽度...
  })
}

// useScrollTracker.js
export function useScrollTracker() {
  const store = useScrollStore()
  const handler = useThrottleFn(() => {
    store.scrollData = calculateScroll()
  }, 300)
  onMounted(() => window.addEventListener('scroll', handler))
  onBeforeUnmount(() => window.removeEventListener('scroll', handler))
}

// useContextMenu.js
export function useContextMenu() { ... }

// useCopyProtection.js
export function useCopyProtection() { ... }
```

### 2.3 全局状态通信改进

**现状**：
- `window.$message()` 作为全局函数，类型不安全，难以追踪
- `window.__showInstallPrompt` 暴露给调试，耦合全局

**建议**：
- Message 改为 `useMessage()` composable，通过 provide/inject 下发（或直接 Pinia）
- InstallPrompt 用 Pinia store 状态控制
- 消除所有 `window` 全局变量

---

## 三、样式系统重构

### 3.1 拆分 SCSS 文件

**现状**：
- `main.scss`（310 行）— 全局变量 + base 样式混在一起
- `post.scss`（844 行）— 文章样式巨大单体

**建议**：

```
style/
├── variables.scss      # CSS 变量、mixin、function
├── reset.scss          # normalize/reset
├── base.scss           # 全局基础样式（body、a、img 等）
├── typography.scss     # 字体 + 排版
├── layout.scss         # 布局相关（header、footer、main）
├── components/         # 组件级样式
│   ├── nav.scss
│   ├── content-card.scss
│   ├── pagination.scss
│   ├── settings.scss
│   ├── right-menu.scss
│   ├── player.scss
│   └── ...
├── post/               # 文章样式按模块拆分
│   ├── base.scss       # 文章基础（h1-h6, p, blockquote）
│   ├── code.scss       # 代码块
│   ├── table.scss      # 表格
│   ├── image.scss      # 图片 + lightbox
│   ├── custom-blocks.scss  # 自定义容器
│   ├── admonitions.scss    # Obsidian 提示
│   └── mobile.scss     # 移动端适配
└── animation.scss      # 动画
```

### 3.2 CSS 变量收敛

**现状**：主题变量 + 自定义项分散在多处通过 JS 直接操作 `document.documentElement`。

**建议**：
- 所有主题变量统一在 `variables.scss` 中定义
- JS 只修改 `store` 状态，通过一个 `useThemeApplier` composable 同步到 DOM
- 消除 `App.vue` 中分散的 `setProperty` 调用

### 3.3 暗色模式优化

**现状**：`App.vue` 中通过 `store.themeType` + `store.themeValue` 手动切换 class。

**建议**：
- 使用 CSS `prefers-color-scheme` media query 作为 fallback
- 用户选择覆盖时通过 `class` 强制
- `<meta name="color-scheme">` 同步，保证系统 UI 一致性

---

## 四、构建与工具链

### 4.1 统一包管理器

**现状**：同时提交 `package-lock.json` 和 `pnpm-lock.yaml`。

**建议**：
- 删除 `package-lock.json`，只保留 `pnpm-lock.yaml`
- `.gitignore` 添加 `package-lock.json` 防止误提交

### 4.2 ESLint 升级

**现状**：`.eslintrc.js` 使用 CJS `module.exports`，与项目 ESM 不一致。

**建议**：
- 迁移到 `eslint.config.mjs`（flat config）
- 配合 `eslint-plugin-vue` 最新版本
- 添加 `@typescript-eslint` 规则（后续 TS 化后启用）

### 4.3 加入测试

**现状**：0 测试。

**建议**：
- 接入 Vitest（与 Vite 共享配置）
- 优先为核心工具函数添加测试：
  - `getAllPosts` 排序逻辑
  - `calculateScroll` 滚动计算
  - `formatTimestamp` 时间格式化
  - `specialDayGray` 日期判断
  - `generateRSS` RSS 生成
- 后续可选 `@vue/test-utils` 测试组件

```js
// vitest.config.ts
import { defineConfig } from 'vitest/config'
export default defineConfig({
  test: {
    include: ['**/*.test.ts']
  }
})
```

### 4.4 添加 CI

**建议**：

```yaml
# .github/workflows/ci.yml
name: CI
on: [push, pull_request]
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v2
      - run: pnpm install
      - run: pnpm lint
  typecheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v2
      - run: pnpm install
      - run: pnpm typecheck
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
      - uses: pnpm/action-setup@v2
      - run: pnpm install
      - run: pnpm build
```

### 4.5 Git Hooks

**现状**：`douyinUrl.js` 需要手动执行。

**建议**：
- 用 `husky` + `lint-staged` 设置 pre-commit hook
- 自动运行 `douyinUrl.js`、ESLint、Prettier

```json
{
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged && node douyinUrl.js pages/posts/2026/"
    }
  },
  "lint-staged": {
    "*.{js,ts,vue,mjs}": ["eslint --fix", "prettier --write"]
  }
}
```

---

## 五、组件细节改进

### 5.1 搜索改进

**现状**：MiniSearch 在客户端加载、索引、搜索。

**建议**：
- 保持 MiniSearch，但将索引生成移到构建时（VitePress 已生成 `@localSearchIndex`）
- 搜索 UI 加防抖（目前是实时 `watch`）
- 搜索结果加键盘导航（上下键选 + 回车跳转）
- 支持搜索标签和分类名

### 5.2 图片懒加载统一

**现状**：同时使用 `loading="lazy"` 原生属性 + `LazyLoader.vue` 自定义组件。

**建议**：
- 全部使用原生 `loading="lazy"`（浏览器支持率 >95%）
- 如需骨架屏效果，用 CSS `@supports` 做渐进增强
- 删除 `LazyLoader.vue`，减少维护量

### 5.3 Markdown 渲染优化

**现状**：`markdownConfig.mjs` 中自定义容器用 `render` 函数手写 HTML。

**建议**：
- 使用 `markdown-it-container` 的模板模式（template）而非手写字符串拼接
- 将 Admonition 渲染改得更语义化，直接用 `<div class="admonition">` 包装
- 考虑支持更多自定义选项（颜色、图标）

### 5.4 分页 URL 模式

**现状**：首页分页用 `page/[num].paths.mjs` 动态生成路由。

**建议**：
- 保持现有架构，但增加对 `?page=2` query 参数的支持（方便分享和外部链接）
- 分类/标签过滤 + 分页的 URL 状态同步

### 5.5 播放器改进

**现状**：APlayer 实例在 `onMounted` 动态创建/销毁。

**建议**：
- 用 `ref` 持有 APlayer 实例，避免重复创建
- 音量持久化到 `localStorage`
- 播放列表支持显示/隐藏
- 歌词滚动同步（如 Meting API 返回歌词）

---

## 六、可维护性

### 6.1 命名规范

**现状**：
- 混合中文/英文变量名：`showSeetings`（拼写错误）、`changeShowStatus`（语义不明）
- 部分组件名不达意：`Control.vue` 实际是控制面板 overlay

**建议**：
- 变量名统一英文
- 拼写修正：`showSeetings` → `showSettings`
- `changeShowStatus` → `toggleModal`
- `Control.vue` → `ControlPanel.vue`

### 6.2 配置系统

**现状**：`themeConfig.mjs` + 根目录 override + `init.mjs` 合并。

**建议**：
- 保持这个机制，挺好
- 加类型定义使覆盖配置有类型提示
- `init.mjs` 增加校验：必填字段缺失时给出清晰错误信息

### 6.3 移除死代码

经分析可安全移除的代码：
- `FooterLink.vue` — `App.vue` 中 `v-if="false"` 从未渲染
- `post.vue` 中 `doc`/`file`/`text`/`pdf` 相关分支（资料模式下可能使用）
- 部分遗留的 `console.log`（已配置 Terser 清除，但开发时仍有）

---

## 七、性能优化

### 7.1 构建产物

| 方向 | 建议 |
|---|---|
| Tree-shaking | 检查 `lodash-es` 是否全量导入 |
| APlayer | 动态 import（`defineAsyncComponent`），首页不加载 |
| Three.js | `SvgTo3d.vue` 用 `defineAsyncComponent` + 仅在侧边栏可用时加载 |
| Fonts | 中文大字体文件建议用 `font-display: swap` + subset 或 CDN 按需加载 |

### 7.2 运行时

| 方向 | 建议 |
|---|---|
| 滚动监听 | 已节流（300ms），建议加 `passive: true` |
| IntersectionObserver | Toc 已用，但可给更多组件复用（如 LazyLoader）|
| DOM 操作 | `App.vue` 中 `onMounted` 批量 DOM 读写建议用 `requestAnimationFrame` 批处理 |

---

## 八、国际化

**现状**：所有 UI 文本硬编码中文。

**建议**：
- 在 `themeConfig.mjs` 中增加 `lang` 字段
- 使用 Vue I18n 或轻量级方案（一个 `strings.json` + 函数查询）
- 至少将导航文字、设置标签、提示消息做成可配置
- 这并非必须，取决于目标用户群体

---

## 九、安全

**现状**：`jumpRedirect` 系统将外部链接通过 `/redirect?url={base64}` 中转。

**建议**：
- 增加 `referrerpolicy="no-referrer"` 防止泄露源站信息
- 确认 base64 解码后做 URL 协议白名单（只允许 `http://`/`https://`）
- 增加 `Content-Security-Policy` header（通过 VitePress 的 `head` 配置）

---

## 十、优先级路线图

| 阶段 | 内容 | 预估工时 |
|---|---|---|
| **P0 核心重构** | TypeScript 化 + Store 拆分 + 大组件拆分 + composable 抽取 | 3~4 周 |
| **P1 工具链** | 测试 + CI + ESLint flat config + Git hooks | 1 周 |
| **P2 样式重构** | SCSS 拆分 + CSS 变量收敛 + 暗色模式优化 | 1~2 周 |
| **P3 功能增强** | 搜索防抖 + 键盘导航 + 播放器改进 + URL query 分页 | 1 周 |
| **P4 品质打磨** | 国际化 + 安全加固 + 死代码清理 + 性能优化 | 1 周 |

**第一阶段（P0）完成后即可获得最大收益**：类型安全、代码可读性提升、组件职责清晰。后续阶段可在日常迭代中逐步推进。

---

*本文档基于 commit `e3aa2f4` 分析，分析日期 2026-07-08。*
